{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,subprocess; d=json.load(sys.stdin); f=d.get('tool_response',{}).get('filePath') or d.get('tool_input',{}).get('file_path',''); subprocess.run(['ruff','check','--fix',f]) if f.endswith('.py') else None\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
